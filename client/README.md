# Todo Client

A Vite + React + TypeScript frontend for the Todos API.

Quickstart:

1. cd client
2. npm install
3. npm run dev

Set `VITE_API_URL` in `.env` if your API is not running on `http://localhost:3000`.